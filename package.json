{
  "name": "flag-pinpoint-duel",
  "version": "1.3.1",
  "private": true,
  "description": "Solo and 1v1 flag guessing game with configurable rules, 2D map and unlabeled precision 3D globe.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22 <23"
  },
  "dependencies": {
    "express": "^5.1.0",
    "flag-icons": "^7.5.0",
    "leaflet": "^1.9.4",
    "socket.io": "^4.8.1",
    "world-countries": "^5.1.0"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
